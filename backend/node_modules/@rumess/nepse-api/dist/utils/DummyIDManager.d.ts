import { MarketStatus } from '../types';
export declare abstract class BaseDummyIDManager {
    protected data: MarketStatus | null;
    protected dummyId: number | null;
    protected dateStamp: Date | null;
    protected marketStatusFunction: (() => Promise<MarketStatus>) | (() => MarketStatus) | null;
    constructor(marketStatusFunction?: (() => Promise<MarketStatus>) | (() => MarketStatus));
    setMarketStatusFunction(func: (() => Promise<MarketStatus>) | (() => MarketStatus) | null): void;
    protected convertToDateTime(dateTimeStr: string): Date;
    toString(): string;
}
export declare class DummyIDManager extends BaseDummyIDManager {
    private updateStarted;
    private updatePromise;
    populateData(force?: boolean): Promise<void>;
    private performUpdate;
    getDummyID(): Promise<number>;
}
//# sourceMappingURL=DummyIDManager.d.ts.map