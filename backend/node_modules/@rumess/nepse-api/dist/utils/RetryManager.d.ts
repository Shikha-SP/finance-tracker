export interface RetryOptions {
    maxRetries?: number;
    baseDelay?: number;
    maxDelay?: number;
    backoffMultiplier?: number;
    retryCondition?: (error: unknown) => boolean;
    onRetry?: (attempt: number, error: unknown, delay: number) => void;
    onMaxRetriesExceeded?: (error: unknown, attempts: number) => void;
}
export declare class RetryManager {
    private static readonly DEFAULT_OPTIONS;
    /**
     * Executes a function with retry logic
     * @param fn The function to execute
     * @param options Retry configuration options
     * @returns Promise that resolves with the function result or rejects after max retries
     */
    static execute<T>(fn: () => Promise<T>, options?: RetryOptions): Promise<T>;
    /**
     * Executes a function with retry logic and continues on failure
     * @param fn The function to execute
     * @param options Retry configuration options
     * @returns Promise that resolves with the function result or null if all retries failed
     */
    static executeWithFallback<T>(fn: () => Promise<T>, options?: RetryOptions): Promise<T | null>;
    /**
     * Utility method to sleep for a given duration
     * @param ms Duration in milliseconds
     */
    private static sleep;
}
/**
 * Convenience function for simple retry operations
 * @param fn The function to execute
 * @param maxRetries Maximum number of retry attempts
 * @param delay Delay between retries in milliseconds
 * @returns Promise that resolves with the function result
 */
export declare function retry<T>(fn: () => Promise<T>, maxRetries?: number, delay?: number): Promise<T>;
/**
 * Convenience function for retry with exponential backoff
 * @param fn The function to execute
 * @param maxRetries Maximum number of retry attempts
 * @param baseDelay Base delay in milliseconds
 * @returns Promise that resolves with the function result
 */
export declare function retryWithBackoff<T>(fn: () => Promise<T>, maxRetries?: number, baseDelay?: number): Promise<T>;
//# sourceMappingURL=RetryManager.d.ts.map