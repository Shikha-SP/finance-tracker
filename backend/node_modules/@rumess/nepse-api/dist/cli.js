#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = require("commander");
const package_json_1 = __importDefault(require("../package.json"));
const dumpToFile_1 = require("./cli/dumpToFile");
const getFloorsheet_1 = require("./cli/getFloorsheet");
const showStatus_1 = require("./cli/showStatus");
const startServer_1 = require("./cli/startServer");
const program = new commander_1.Command();
program
    .name('nepse-cli')
    .description('Command line interface to nepalstock.com')
    .version(package_json_1.default.version);
// Remove the global options and create separate commands
program
    .command('server')
    .description('Starts local server at 0.0.0.0:8000')
    .action(async () => {
    await (0, startServer_1.startServer)();
});
program
    .command('status')
    .description('Dumps Nepse status to the standard output')
    .option('-o, --output-file <file>', 'sets the output file for dumping the content')
    .option('-csv, --to-csv', 'sets the output format from default[JSON] to CSV')
    .action(async (options) => {
    const outputContent = await (0, showStatus_1.showStatus)();
    if (outputContent) {
        (0, dumpToFile_1.dumpToFile)(options.outputFile, outputContent, options.toCsv);
    }
});
program
    .command('floorsheet')
    .description("Dumps Nepse's floorsheet to the standard output")
    .option('-o, --output-file <file>', 'sets the output file for dumping the content')
    .option('-csv, --to-csv', 'sets the output format from default[JSON] to CSV')
    .option('-hide, --hide-progressbar', 'sets the visibility of progress base to False')
    .action(async (options) => {
    const outputContent = await (0, getFloorsheet_1.getFloorSheet)(options.hideProgressbar);
    if (outputContent) {
        (0, dumpToFile_1.dumpToFile)(options.outputFile, outputContent, options.toCsv);
    }
});
program.showHelpAfterError();
program.parse();
//# sourceMappingURL=cli.js.map